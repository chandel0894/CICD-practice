git init - to initiliaze the git the project directory
git clone - to clone the remote into local 
git add remote origin URL_name - to add the remote origin
git status - to check the changed files that you have worked
git config user.name "chandel0894" - to set the username
git config user.name - to the username 
git remote -v

If error is Permission denied 403 and replace token with the actual token 
git remote set-url origin https://chandel0894:<YOUR_TOKEN>@github.com/chandel0894/CICD-practice.git

git branch ( to show all the brances)